<h1>Lists#show</h1>
<p>Find me in app/views/lists/show.html.erb</p>

<h1><%= @list.name %></h1>

 <%= link_to 'Edit the List', edit_list_path %>

 <%= link_to "Remove",
                  list_path(@list),
                  method: :delete,
                  data: { confirm: "Are you sure?" } %>

 <%= link_to 'Return to index', lists_path %>
